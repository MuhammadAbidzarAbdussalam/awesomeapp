<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abidzar.awesomeapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AwesomeApp">
        <activity
            android:name=".view.DetailScrollingActivity"
            android:exported="true"
            android:label="@string/title_activity_detail_scrolling"
            android:theme="@style/Theme.AwesomeApp.NoActionBar" />
        <activity
            android:name=".view.DetailPhotoActivity"
            android:exported="true"
            android:theme="@style/Theme.AwesomeApp.NoActionBar" />
        <activity
            android:name=".view.ScrollingActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AwesomeApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>